node_modules
.next
.env*.local
.env.production
.env.development
.DS_Store
*.log

# Development logs
logs/

# Lock files (if switching package managers)
package-lock.json
yarn.lock

# TypeScript incremental build artifact (regenerated every build)
tsconfig.tsbuildinfo

# Old page backups
*.backup
*.backup-old

# Cache directories
node_modules/.cache
.next/cache

# Image files
*.tif
*.TIF

# Security sensitive files
*.pem
*.key
*.cert
.env
content/cmq-headshots-seo-*.json
# Google credentials — never commit (belt-and-suspenders; keep these outside the repo)
client_secret*.json
*.apps.googleusercontent.com.json
*-service-account*.json

# Journal (local session records)
journal/

# IDE
.vscode/*
!.vscode/extensions.json
.idea
*.swp
*.swo
*~
out/
.next/
public/images/CMQ HEADSHOT WELCOME MESSAGE.mp4
travel - wb - 1600/
family photos /
