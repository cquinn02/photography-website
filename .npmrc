shamefully-hoist=true
strict-peer-dependencies=false

# Allow build scripts for these packages (required for Next.js image optimization)
; pnpm v10+ uses a new approval system, this is for older versions
side-effects-cache=true
